# 지도를 카카오 / 네이버 / 구글로 바꾸기

지금은 **OpenStreetMap**을 쓰고 있습니다. 키가 없어도 되기 때문입니다.
카카오·네이버·구글로 바꾸려면 **키 한 줄만 넣으면** 됩니다.

`index.html` 안에서 `const MAP_KEY` 를 찾으면 이 부분이 나옵니다.

```js
const MAP_KEY = {
  kakao:  "",   // 카카오 JavaScript 키
  naver:  "",   // 네이버 지도 Key ID
  google: ""    // 구글 Maps API 키
};
```

따옴표 안에 키를 넣고 저장하면 끝입니다. 셋 다 넣으면 카카오 → 네이버 → 구글 순으로 씁니다.

---

## 카카오맵 — 가장 추천 (무료, 카드 등록 없음)

1. <https://developers.kakao.com> 로그인 → **내 애플리케이션** → **애플리케이션 추가하기**
2. 앱 이름은 아무거나 (예: `숙소지도`)
3. 만든 앱 → **앱 키** → **JavaScript 키** 복사
4. 같은 앱 → **플랫폼** → **Web** → 사이트 도메인에 아래 두 줄 등록

   ```
   https://mrsure1.github.io
   http://localhost
   ```

5. 복사한 키를 `kakao:` 에 붙여넣기

> 4번 도메인 등록을 빠뜨리면 지도가 안 뜹니다. 꼭 하세요.

---

## 네이버 지도 (무료, 가입 시 카드 등록 필요)

1. <https://console.ncloud.com> 가입 → **Services** → **AI·NAVER API** → **Maps** 이용 신청
2. **Application 등록** → Web Dynamic Map 선택
3. **Web 서비스 URL** 에 `https://mrsure1.github.io` 등록
4. 발급된 **Key ID** 를 `naver:` 에 붙여넣기

---

## 구글 지도 (카드 등록 필요, 소규모는 사실상 무료)

1. <https://console.cloud.google.com> → 프로젝트 만들기 → 결제 계정 연결
2. **API 및 서비스** → **Maps JavaScript API** 사용 설정
3. **사용자 인증 정보** → **API 키 만들기**
4. 만든 키 → **애플리케이션 제한사항** → **HTTP 리퍼러** → `https://mrsure1.github.io/*` 추가
5. 키를 `google:` 에 붙여넣기

> 4번 제한을 걸지 않으면 키가 도용될 수 있습니다. 꼭 거세요.

---

## 키 없이 구글 지도를 쓰는 방법

**구글 내 지도(My Maps)** 로 핀을 직접 찍고 그 지도를 삽입하는 방법도 있습니다.

1. <https://www.google.com/mymaps> → **새 지도 만들기**
2. 숙소 12곳을 검색해 핀을 찍고 색을 빨강으로 바꾸기
3. **공유** → 링크가 있는 모든 사용자에게 공개
4. **⋮ 메뉴** → **내 사이트에 삽입** → `<iframe ...>` 주소 복사
5. 그 주소를 주시면 지도 자리에 넣어 드립니다

손은 조금 가지만 키 발급이 필요 없고, 핀 위치를 직접 손볼 수 있습니다.
